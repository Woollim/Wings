<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorNo1</item>
        <item name="colorPrimaryDark">@color/colorNo2</item>
        <item name="colorAccent">@color/colorNo3</item>
        <item name="android:fontFamily">@font/nanum</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorNo1</item>
        <item name="colorPrimaryDark">@color/colorNo2</item>
        <item name="colorAccent">@color/colorNo3</item>
        <item name="android:background">@drawable/all_splash_view</item>
    </style>

</resources>
